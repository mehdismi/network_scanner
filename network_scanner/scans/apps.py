"""App configuration for the scans application."""
from django.apps import AppConfig

class ScansConfig(AppConfig):
    """Configuration class for the scans app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'scans'
